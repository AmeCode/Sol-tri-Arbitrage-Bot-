version: "3.9"
services:
  tri_arb:
    build: .
    container_name: tri_arb
    restart: unless-stopped
    env_file: .env
    ports:
      - "${METRICS_PORT:-9102}:9102"
    ulimits:
      nofile:
        soft: 65536
        hard: 65536
